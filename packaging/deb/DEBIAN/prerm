#!/bin/bash
# Pre-removal script for spaceblack .deb package

set -e

# Remove the ghost CLI launcher
rm -f /usr/local/bin/ghost

# Clean up the virtual environment (large, not needed after uninstall)
rm -rf /opt/spaceblack/.venv

echo "Space Black has been removed."
